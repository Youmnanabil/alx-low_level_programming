#include <stdio.h>
/*
 * main - Entry point
 *
 * Description: a c program prints numbers
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int num;

	for(num = 0; num < 10; num++)
		printf("%d \n", num);
	return (0);
}
