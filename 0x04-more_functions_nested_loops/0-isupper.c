#include "main.h"
/**
 * _isupper - check the code
 * @c: is the character
 * Return: 1 if it is upper, 0 if not
 */
int _isupper(int c)
{
	int c = 65;

	if (c <= 90)
		return (1);
	else
		return (0);
}
